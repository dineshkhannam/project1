apiVersion: apps/v1
kind: Deployment
metadata:
  name: static-web-deployment
  namespace: sample
spec:
  replicas: 3
  template:
    metadata:
      labels:
        app: days-app
    spec:
      imagePullSecrets:
      - name: github-container-registry-login
      containers:
      - name: days-app-container
        image: ghcr.io/dineshkhannam/gitops01:abad17b8cb6e0714753df2dc2d1b3e96dd9d1c43
        imagePullPolicy: Always
        ports:
        - containerPort: 80
  selector:
    matchLabels:
      app: days-app
