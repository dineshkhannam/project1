apiVersion: apps/v1
kind: Deployment
metadata:
  name: static-web-deployment
  namespace: sample
spec:
  replicas: 3
  template:
    metadata:
      labels:
        app: days-app
    spec:
      imagePullSecrets:
      - name: github-container-registry-login
      containers:
      - name: days-app-container
        image: ghcr.io/dineshkhannam/gitops01:0caab22e701a2cc402739d68a6d648aada95d7e2
        imagePullPolicy: Always
        ports:
        - containerPort: 80
  selector:
    matchLabels:
      app: days-app
